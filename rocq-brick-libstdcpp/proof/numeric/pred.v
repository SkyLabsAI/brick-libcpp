
Require Export skylabs.brick.libstdcpp.numeric.model.

(** [std::gcd] and [std::lcm] are pure value computations and own no memory, so this family exports no separation predicate. *)
(* Bootstrap file; substantive edits are made through the live rocq-ed session. *)
